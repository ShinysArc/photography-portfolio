# -----------------------------
# OS / Editor junk
# -----------------------------
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.idea/
.vscode/

# -----------------------------
# Root env files (compose, etc.)
# -----------------------------
.env
.env.*
!.env.example

# -----------------------------
# Logs
# -----------------------------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# =============================
# Web
# =============================
/web/node_modules/
/web/.next/
/web/out/
/web/dist/
/web/.turbo/
/web/.vercel/
/web/coverage/
/web/.eslintcache
/web/next-env.d.ts

# Web env files (keep the example)
web/.env
web/.env.*
!web/.env.local.example

/web/build/
# /web/**/*.js
# /web/**/*.js.map

# =============================
# Server
# =============================
/server/bin/
/server/build/
/server/tmp/
/server/coverage/

# Go module/cache artifacts (vendor only if you use `go mod vendor`)
/server/vendor/

# Server data (cache.json + media files written by the app)
# MEDIA_DIR usually lives here (e.g., /server/data/media)
server/data/

# Server env files (keep the example)
server/.env
server/.env.*
!server/.env.example

# -----------------------------
# Docker/containers (no build artifacts kept)
# -----------------------------
# If you ever export images or compose env, ignore here
# docker-compose.override.yml
# docker/.env
